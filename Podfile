# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'cocoapoding' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for cocoapoding
    pod "SVProgressHUD"
    pod "SwiftyJSON"

  target 'cocoapodingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'cocoapodingUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
